'use strict';

/**
 * question-truefalse router
 */

const { createCoreRouter } = require('@strapi/strapi').factories;

module.exports = createCoreRouter('api::question-truefalse.question-truefalse');
