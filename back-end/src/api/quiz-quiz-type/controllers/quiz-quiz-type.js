'use strict';

/**
 * quiz-quiz-type controller
 */

const { createCoreController } = require('@strapi/strapi').factories;

module.exports = createCoreController('api::quiz-quiz-type.quiz-quiz-type');
